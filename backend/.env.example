# Скопируй в .env и заполни. Файл .env в git не коммитится.

# Подключение к PostgreSQL (совпадает с docker-compose.yml)
DATABASE_URL=postgresql+psycopg://kitfinder:kitfinder@localhost:5432/kitfinder

# Аутентификация админа (появится на этапе M1)
# ADMIN_PASSWORD_HASH — bcrypt-хеш пароля, сгенерировать один раз
ADMIN_PASSWORD_HASH=
# SECRET_KEY — для подписи cookie-сессии, случайная строка
SECRET_KEY=

# S3-совместимое хранилище фото китов (бакет должен допускать public-read или CDN).
# Фото раздаются по S3_PUBLIC_BASE_URL/<object_key>.
# Значения ниже — под локальный MinIO из docker-compose (бакет kit-images создаётся
# сервисом minio-init). В проде заменить на реальный S3/CDN.
S3_ENDPOINT_URL=http://localhost:9000
S3_BUCKET=kit-images
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_REGION=us-east-1
S3_PUBLIC_BASE_URL=http://localhost:9000/kit-images
